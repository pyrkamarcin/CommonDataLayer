syntax = "proto2";
package materializer;
import "common.proto";

service Materializer {
  rpc ValidateOptions(Options) returns (Empty);
  rpc UpsertView(common.MaterializedView) returns (Empty);
}

message Options {
  required string options = 1;
}

message Empty {}
